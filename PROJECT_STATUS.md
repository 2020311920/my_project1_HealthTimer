# 🏋️ HealthTimer (timer_app) 프로젝트 상태 및 개발 일지

**[중요]** 이 파일은 HealthTimer 프로젝트의 '두뇌'와 같습니다. AI 어시스턴트(Gemini Code Assist)가 이전 대화, 개발 내역, 그리고 앞으로의 계획을 모두 기억하고 연속적인 개발을 지원할 수 있도록, **모든 대화와 개발 과정의 요약을 여기에 계속해서 업데이트하고 기록해주세요.**

새로운 대화를 시작할 때, 이 파일의 전체 내용을 가장 먼저 제공하면, AI는 모든 맥락을 이해하고 마치 이전 대화가 끊기지 않은 것처럼 작업을 이어나갈 것입니다.

---

## 🚀 최종 목표 (Ultimate Goal)

1.  **다중 소셜 로그인**: 구글, 카카오, 네이버 등 주요 소셜 로그인을 지원합니다.
2.  **크로스 플랫폼 배포**: 1순위로 구글 플레이스토어에 출시하고, 이후 애플 앱스토어에도 등록합니다.
3.  **핵심 기능 고도화**: 사용자는 타이머를 통해 운동을 기록하고, 저장된 기록을 언제든지 불러오거나 수정할 수 있습니다.
4.  **SNS 공유 기능**: 감각적인 디자인을 기반으로, 앱 내에서 카메라로 운동 완료 사진을 촬영하고, 운동 기록과 함께 예쁘게 배치하여 인스타그램 등 다른 SNS에 손쉽게 공유할 수 있는 기능을 제공합니다.

---

## 📌 현재 개발 상태 (Current Status)
- **타이머 핵심 로직 (`workout_timer_provider.dart`)**: 세트 진행, 휴식 시간 관리, 다음/이전 상태 전환, 오디오 비프음 등 기능 구현 완료.
- **백그라운드 지원 (`timer_screen.dart`)**: `flutter_foreground_task`를 이용해 백그라운드 전환 시 포그라운드 서비스 및 알림 생성, 알림 창 내 제어(일시정지, 재시작 등) 기능 구현 완료.
- **Firebase 연동 및 프로필 (`profile_screen.dart`)**: Firebase Auth 및 Firestore 연동. 타임라인 기록 열람, 종목 이름 수정, 기록 삭제, 닉네임 수정, **프로필 이미지 변경 기능 구현 완료**.
- **데이터 모델**: `WorkoutRecord` 및 `ExerciseSet` 모델 구현, Firestore와 기기 로컬 `SharedPreferences`에 데이터를 병렬로 저장하는 구조 구비.
- **버그 픽스 완료**: 운동 종료 시 타이머 데이터 완전 초기화 적용 및 UI 개선(ElevatedButton 적용) 완료. 닉네임 변경 즉시 렌더링 반영 로직 추가 완료.
- **계정별 데이터 격리 완료**: 사용자의 `uid`를 기준으로 `SharedPreferences` 키를 분리하고, Firestore의 `users/{uid}/settings`에 타이머 설정을 양방향 동기화하여 다중 로그인 시에도 개별 설정이 유지되도록 구축.
- **설정 기능 확장**: 운동 종목을 사용자가 직접 추가/삭제/순서 변경할 수 있는 '운동 종목 관리' 기능 구현 완료.
- **운동 루틴 폴더화 및 타이머 연동 완료**: `운동 부위(폴더) -> 운동 종목` 구조 개편 완료. '기본 운동' 루틴 보호 및 루틴 이름 수정 지원. 종료된 운동 기록 타임라인에 해당 루틴 이름을 표기하여 편의성 대폭 강화.
- **캘린더 뷰 구축 완료 (`calendar_screen.dart`)**: `table_calendar` 패키지를 연동하여 운동한 날에 마커 표시(잔디 심기) 및 특정 날짜의 기록 필터링 기능 구현.

## 🚧 미완성 및 향후 개발 항목 (To-Do)
1. **SNS 공유 기능 준비 (카메라 연동)**
   - **[전략 수립 완료]**: 프로필 및 캘린더 화면의 운동 기록을 시각적인 이미지 카드로 만들어 공유.
   - **기술 스택**: `RepaintBoundary`(위젯 캡처), `path_provider`(임시 저장), `share_plus`(OS 공유 시트 호출).
   - **진행 스텝**: 패키지 설치 -> 공유 UI(`ShareScreen`) 디자인 -> 캡처 및 공유 로직 구현 -> 기존 타임라인에 공유 버튼 연동.

## 🛠️ 다음 개발 스텝 (Next Steps)
1. **SNS 쉐어 패키지 세팅**: `pubspec.yaml`에 `share_plus`, `path_provider`를 추가.
2. **ShareScreen UI 구축**: 선택한 사진 배경 위에 운동 데이터(시간, 부위, 세트)를 인스타 감성으로 배치하는 화면 뼈대 생성.

---
*마지막 업데이트: 2026-05-06*

**[참고]** 
프로젝트 세팅 이슈: Firebase Console 내에서 `Firestore Database`와 `Storage`를 활성화(Get Started)해야 프로필 이미지 저장 및 운동 기록 동기화가 정상 작동함.